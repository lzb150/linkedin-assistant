{
  "name": "linkedin-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Draft-only LinkedIn message assistant and job radar: reads your own inbox, scores job messages against your resume, writes draft replies; finds matching vacancies on LinkedIn/DOU/Djinni and prepares application packages. Never sends or applies.",
  "scripts": {
    "login": "node login.mjs",
    "check": "node check.mjs",
    "jobs": "node jobs.mjs",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  }
}
